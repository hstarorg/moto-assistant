# AGENTS.md

## Git

- 禁止执行任何 Git 写操作，也不得修改 `.git/` 或改变 staged/unstaged 状态。
- 仅允许执行 Git 只读操作，用于查看仓库状态、差异和历史。
- 如需 Git 写操作，只提供建议命令，由用户自行执行。

## 修改原则

- 只修改任务需要的文件，保留无关的用户改动。
- 修改前先阅读相关代码和配置，不根据过时说明猜测当前实现。
- 遵循现有架构、命名和格式，避免无关重构、格式化或依赖升级。
- 数据库访问优先级为 ORM（Repository/Entity API）→ QueryBuilder → 原生 query；仅当前一级无法清晰、安全地实现时才降级。
- 未经明确授权，不删除、覆盖或迁移用户数据和文件。
- 接口、字段或配置发生变化时，同步检查所有受影响的调用方。
- 不提交依赖目录、生成物、缓存和本地私有配置。

## 工具与验证

- Node.js 和 pnpm 使用用户的全局交互式 zsh 环境，并在 sandbox 外运行；不要使用 Codex fallback runtime 或硬编码 Node 路径。
- 修改后执行与风险匹配的静态检查、构建或测试。
- 不得声称未实际执行的检查通过；无法验证时明确说明原因和待验证项。

## 安全

- secrets 使用环境变量或专用密钥管理服务，不写入客户端代码、仓库、日志或面向用户的错误信息。
- 示例配置只能使用明显的占位值，不复制真实凭据。
- 所有外部输入都必须校验；涉及身份和资源访问时必须检查授权与资源归属。
